---
name: deploy

on:
  workflow_run:
    workflows:
      - Tests
      - E2E Tests
    types:
      - completed
    branches:
      - main

env:
  HELM_REGISTRY: registry-1.docker.io
  PLATFORMS: linux/amd64,linux/arm64

jobs:
  semver:
    permissions: write-all
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.tagging.outputs.new_tag }}
    steps:
    - uses: actions/checkout@v5
      with:
        fetch-depth: '0'
    - name: Bump version and push tag
      uses: anothrNick/github-tag-action@1.75.0
      id: tagging
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        WITH_V: true
        RELEASE_BRANCHES: main
        DEFAULT_BUMP: patch
    - name: Run GoReleaser
      uses: goreleaser/goreleaser-action@v6
      with:
        version: latest
        args: release --clean
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  helm:
    runs-on: ubuntu-latest
    needs: semver
    steps:
      - name: Install Helm
        run: |
          curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-4 | bash
      - name: Helm DockerHub Login
        run: echo ${{ secrets.DOCKERHUB_TOKEN }} | helm registry login $HELM_REGISTRY -u ${{ github.repository_owner }} --password-stdin
      - name: Package Helm Chart
        run: make pkg-helm-chart VERSION=${{ needs.semver.outputs.tag }}
      - name: Push Helm Chart to DockerHub
        run: helm push dist/*.tgz oci://$HELM_REGISTRY/${{ github.repository_owner }}
  docker:
    runs-on: ubuntu-latest
    needs: semver
    steps:
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ github.repository_owner }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and push latest
        id: docker_build_latest
        uses: docker/build-push-action@v6
        with:
          push: true
          tags: ${{ github.repository }}:latest
          platforms: ${{ env.PLATFORMS }}
      - name: Build and push versioned
        id: docker_build_versioned
        uses: docker/build-push-action@v6
        with:
          push: true
          tags: ${{ github.repository }}:${{ needs.semver.outputs.tag }}
          platforms: ${{ env.PLATFORMS }}